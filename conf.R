suppressPackageStartupMessages({
  library("mcmcdb")
  library("stanmisc")
})

ROOT_DIR <- normalizePath(".")
STAN_MODEL_DIR <- file.path(ROOT_DIR, "stan/models")
STAN_OUTPUT_DIR <- file.path(ROOT_DIR, "stan/output")

jrnold_sbreak_run_stan <- function(...) {
  
}

jrnold_sbreak_run_stan <- function(...) {
  
}
